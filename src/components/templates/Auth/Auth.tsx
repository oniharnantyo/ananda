import LoginForm from '@components/organisms/Login/Login';

import AuthLayout from '../Layout/AuthLayout';

const Auth = () => {
  return (
    <AuthLayout>
      <LoginForm />
    </AuthLayout>
  );
};

export default Auth;
