import { Container, Navbar } from "components";

function Home() {
    return (
        <Container>
            <Navbar content={'dis home page'} currentPage={'event'} />
        </Container>
    );
};
export default Home;
